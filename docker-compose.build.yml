# Build overlay: builds the app image locally as tld-buddy:local.
# Run this on the Docker host BEFORE deploying the Portainer Git stack.
# Usage: docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  app:
    build: .
    image: tld-buddy:local
